// Re-export everything from customFetcher for backwards compatibility
export * from './customFetcher';

// Also export serverFetcher for server-side usage
export { serverFetcher } from './serverFetcher';
