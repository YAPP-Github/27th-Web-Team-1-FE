export const CAMERA = {
  /** 모바일 카메라 권장 해상도 (Full HD 세로) width */
  VIDEO_WIDTH: 1080,
  /** 모바일 카메라 권장 해상도 (Full HD 세로) height */
  VIDEO_HEIGHT: 1920,
  /** 이미지 MIME 타입 */
  IMAGE_TYPE: 'image/jpeg',
  /** JPEG 이미지 품질 (0.0 ~ 1.0) */
  JPEG_QUALITY: 0.75,
} as const;
